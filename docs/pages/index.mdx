# Edge Design

A comprehensive design system for the Edge Platform — providing a consistent design language, reusable components, and composable patterns for building cloud-edge management interfaces.

## Features

- **Design Tokens** — Colors, typography, spacing, and motion tokens in CSS variables, Tailwind preset, and JS constants
- **6 Built-in Themes** — Classic Blue, Purple, Green, Orange, Dark, and Cyan with custom theme extensibility
- **50+ Components** — Core UI components built on Radix UI with full accessibility support
- **Domain Components** — Edge-specific selectors, monitoring dashboards, topology visualization
- **Hooks Library** — Reusable React hooks for state management, UI interactions, and utilities
- **Pluggable Architecture** — Token-first design enables complete rebranding; layered packages allow selective adoption

## Packages

| Package | Description |
|---------|-------------|
| `@edge/tokens` | Design tokens — CSS variables, Tailwind preset, JS/TS constants |
| `@edge/ui` | Core UI components — Button, Input, Dialog, Table, Card, etc. |
| `@edge/edge-components` | Domain components — Selectors, Monitoring, Topology, Terminal |
| `@edge/hooks` | Shared hooks — State management, UI, DOM utilities |
| `@edge/icons` | Icon system — Lucide React + custom Edge icons |

## Quick Start

### Install

```bash
pnpm add @edge/ui @edge/tokens
```

### Use the Tailwind Preset

```js
// tailwind.config.js
const edgePreset = require('@edge/tokens/tailwind-preset')

module.exports = {
  presets: [edgePreset],
  content: ['./src/**/*.{ts,tsx}'],
}
```

### Use Components

```tsx
import { Button, Card, Badge } from '@edge/ui'

function App() {
  return (
    <Card>
      <Badge variant="success">Running</Badge>
      <Button>Deploy</Button>
    </Card>
  )
}
```

## Documentation

- [Guide](/guide) — Introduction, installation, and getting started
- [Customization](/customization) — Colors, themes, typography, spacing, motion
- [Components](/components) — Core UI component library
- [Domain Components](/domain) — Edge Platform-specific components
- [Hooks](/hooks) — Reusable React hooks
- [Icons](/icons) — Icon system and gallery
- [Patterns](/patterns) — Composition patterns and templates
