# Installation

## Prerequisites

- Node.js 18+
- pnpm 8+

## Install Packages

```bash
# Core tokens + UI components
pnpm add @edge/tokens @edge/ui

# Domain components (optional)
pnpm add @edge/edge-components

# Hooks (optional)
pnpm add @edge/hooks
```

## Configure Tailwind CSS

Add the Edge Design Tailwind preset to your `tailwind.config.js`:

```js
const edgePreset = require('@edge/tokens/tailwind-preset')

module.exports = {
  presets: [edgePreset],
  content: [
    './src/**/*.{ts,tsx}',
    './node_modules/@edge/ui/dist/**/*.{js,mjs}',
  ],
}
```

The preset automatically:
- Injects all CSS custom properties (`:root` and `.dark`)
- Configures semantic color utilities (`bg-primary`, `text-muted-foreground`, etc.)
- Sets up border radius, shadows, z-index, and animation tokens
- Adds base styles for `body` and border colors

## Import Styles

Import the component styles in your app entry:

```tsx
// app/layout.tsx or main.tsx
import '@edge/ui/styles.css'
```

## Verify

```tsx
import { Button } from '@edge/ui'

export default function App() {
  return <Button variant="default">It works</Button>
}
```
